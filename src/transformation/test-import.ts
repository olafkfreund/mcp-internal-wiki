// Test import file to verify TemplateEngine can be imported
import { TemplateEngine } from './TemplateEngine';

// Simple test to verify the import works
const engine = new TemplateEngine('./templates');
console.log('TemplateEngine imported successfully');
